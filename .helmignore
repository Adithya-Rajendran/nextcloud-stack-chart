# Patterns ignored when packaging the chart.
.DS_Store
.git/
.gitignore
.bzr/
.hg/
.svn/
*.swp
*.bak
*.tmp
*.orig
*~
.project
.idea/
.vscode/
*.tmproj
.github/
# Secrets we never want to ship inside the package.
secrets/
*.secret.yaml
# Audit / review artifacts (transient).
AUDIT.md
