{{/* Removed: chart no longer generates Secrets or runs a post-install
     schema-fix Job. External secrets only — see scripts/bootstrap-secrets.sh.
     File kept (empty) because the workspace fs doesn't permit file deletion;
     excluded from package by .helmignore. */}}
